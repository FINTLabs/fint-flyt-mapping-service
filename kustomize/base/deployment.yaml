apiVersion: apps/v1
kind: Deployment
metadata:
  name: fint-flyt-mapping-service
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    spec:
      restartPolicy: Always
      volumes:
        - name: credentials
          secret:
            secretName: fint-flyt-mapping-service-kafka-certificates
      containers:
        - image: ghcr.io/fintlabs/fint-flyt-mapping-service:sha-0000000
          name: fint-flyt-mapping-service
          volumeMounts:
            - name: credentials
              mountPath: /credentials
          resources:
            limits:
              memory: "2048Mi"
              cpu: "1"
            requests:
              memory: "512Mi"
              cpu: "100m"
          ports:
            - containerPort: 8080
          envFrom:
            - secretRef:
                name: fint-flyt-mapping-service-kafka
            - configMapRef:
                name: fint-flyt-mapping-service